
You are an expert TypeScript React developer. Your task is to analyze the provided Markdown documentation and generate a production-ready, fully functional React TSX component based on its specifications.
Requirements:
1. TypeScript & Types: Use strict TypeScript. Define explicit Interfaces/Types for Props and State. Do not use `any`.
2. JSDoc: Add comprehensive JSDoc comments for the component definition, props interfaces, and any complex custom types.
3. React Best Practices: Use modern functional components and hooks. Destructure props. Ensure proper naming conventions (PascalCase for components).
4. Reusability: Make the component highly reusable and composable. Use sensible default props and optional properties where appropriate.
5. Styling: Use standard Tailwind CSS utility classes for styling (or plain CSS if no styling framework is specified in the docs). Do not use inline styles unless dynamic.
6. Edge Cases: Handle loading states, empty states, or missing optional props gracefully.
Output Rules:
- Output ONLY valid TSX code. 
- Do NOT include conversational filler, explanations, or markdown code block wrappers (like ```tsx) around the code.
- Structure the file in this exact order:
  1. Imports (React, third-party, local)
  2. Type/Interface definitions
  3. Component definition (with JSDoc)
  4. Export
Input documentation:
{DOCS}
